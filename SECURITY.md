# 安全策略

## 支持范围

当前维护分支为 `main`，正式验证平台为 Windows 10/11 x64。请优先升级到
GitHub Releases 中的最新版本。

## 报告安全问题

请不要在公开 Issue 中提交密钥、论文原文、个人路径或包含隐私的日志。可先在
Issue 中仅描述影响范围，并注明“安全问题”；维护者会协调后续的最小披露方式。

## 本地数据与网络

-   论文、摘录、笔记、标签、翻译缓存和 FTS5 全文检索索引默认保存在本机。
-   翻译和视觉理解默认访问用户配置的 Ollama 地址，默认值为
    `http://127.0.0.1:11434`。
-   程序不会静默安装 Ollama 或下载模型，安装与约 6.1 GB 的模型下载都需要用户确认。
-   发布安装包目前未进行商业代码签名，Windows SmartScreen 可能显示提示；请只从本仓库
    Releases 下载，并核对发布页提供的 SHA-256。
