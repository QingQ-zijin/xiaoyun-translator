# 更新日志

## 4.5.4（2026-07-28）

### 文献整理与排序

-   新增“一键归档当前结果”，无需逐项勾选；只处理当前项目、搜索与标签筛选后的可见文献。
-   新增“最近打开、最久未打开、最近导入、最早导入”四种排序，并记住用户上次选择。
-   数据库独立记录文献导入时间与最近打开时间，重启后仍可准确排序。
-   文献列表显示导入日期和最近打开日期，排序依据更加清楚。
-   一键归档复用原子批量事务、重复操作防护与 500 篇安全上限。

## 4.5.3（2026-07-27）

### 文献批量管理

-   新增当前筛选结果的逐项选择、全选和半选状态，支持批量归档、取消归档、移入回收站与恢复。
-   新增独立“已归档”视图、数量统计、归档日期徽章、紫色左侧强调和淡紫背景。
-   归档与回收站使用独立且互斥的生命周期；批量事务遇到缺失论文会整体回滚，不产生半完成状态。
-   活动论文计数、最近阅读恢复、论文切换器、引用匹配和后台概要队列均排除归档文献。
-   小窗口保留归档、回收、恢复和永久删除等必要操作；归档文献仍可打开阅读，并在顶部切换器中明确标记。

### 签名应用内更新

-   启动后非阻塞地静默检查 GitHub Release；发现新版本时显示可稍后关闭的更新通知。
-   设置页新增“软件更新”，支持手动检查、版本与发布说明、下载进度、失败重试和安装后重启。
-   接入 Tauri 2 updater/process 插件和主窗口最小权限；更新包必须通过仓库专用签名验证。
-   Windows Release 先创建草稿，校验安装包、签名、`latest.json`、版本、URL 与 SHA-256 后才正式发布。
-   发布工作流固定第三方 Action 提交，只在签名步骤注入私钥，并校验标签与三处应用版本一致。

### Ollama 自动恢复

-   应用启动、设置重新开启、Ctrl+D 翻译和 Ctrl+E OCR 共用单飞恢复流程；Ollama 被退出后会自动重新启动服务并加载 Gemma 4。
-   首次请求连接被拒绝时仅恢复并重试一次；正常热路径不增加健康探测，不影响原有首字速度。
-   多个请求同时发现服务离线时共享一个后台恢复任务，只会创建一个 Ollama 进程；关闭弹窗可立即取消当前翻译，但不会破坏后台启动。
-   快捷翻译、主翻译页和论文划词浮窗会显示“正在自动启动本地 AI”，论文冷启动使用独立的 130 秒保护时限。
-   用户明确关闭本地 AI 后，设置页不再擅自重新启动 Ollama。

> 4.5.3 是首个内置签名更新器的桥接版本。4.5.2 及更早版本需要手动安装本版一次；后续版本可在应用内一键更新。

## 4.5.2（2026-07-23）

### 零基础 Ollama 接入

-   首次启动会主动显示本地 AI 接入向导，不再要求用户先找到设置页。
-   清楚说明 Ollama 是独立运行的本地 AI 程序，无需账号、API Key 或命令行知识。
-   安装 Ollama 后自动轮询状态，并在 Windows 与 macOS 上尝试启动本地服务。
-   模型下载前明确提示约 6.1 GB 下载量与磁盘空间要求，显示实时进度，支持取消和断点复用。
-   只有模型真正载入内存后才显示“本地 AI 已准备好”；模型已下载但未加载时提供一键恢复。
-   优化网络、磁盘、权限和超时错误提示，后台刷新不再吞掉当前错误。
-   翻译页显示真实后端状态，未就绪时可直接进入设置完成接入。

### 稳定性与平台行为

-   改进模型下载全阶段取消逻辑，覆盖连接、下载、检测与预热阶段。
-   应用启动后继续自动恢复 Ollama 服务与 Gemma 4 E4B 模型。
-   Linux 不再隐式启动后台进程，改为显示明确的 systemd 或 `ollama serve` 指引。
-   补充首次接入、故障恢复和记事本划词翻译验收教程。

## 4.5.0（2026-07-23）

这是“小允翻译”首次公开发布版本。

### 学术翻译

-   全局 `Ctrl+D` 划词翻译与 `Ctrl+E` 截图翻译。
-   常驻取词辅助进程与窗口状态机，连续请求只保留最新任务，异常时自动恢复。
-   Gemma 4 E4B 本地流式翻译，保留 Markdown、LaTeX、变量、引文和单位。
-   根据论文标题、上下文和术语表进行学术领域消歧。
-   本地 Windows 朗读，不依赖远程 TTS 服务。

### AI 论文与书籍阅读器

-   导入 PDF、扫描 PDF、Markdown、DOCX 和 TeX。
-   PDF 文本层、目录、页码、搜索、触摸板缩放、阅读进度恢复与长文档虚拟化。
-   论文概要、关键术语、摘录、彩色高亮、笔记、标签和项目分类。
-   书籍模式支持目录树、章节跳转、章节摘要和章节术语。
-   扫描页可使用 Gemma 4 多模态 OCR 与公式、图表理解。

### 本地 AI

-   翻译、OCR、论文概要和问答共用一个 Gemma 4 E4B 模型，避免多模型常驻显存。
-   设置页提供 Ollama 接入向导、服务检测、模型下载进度、取消和重试。

### 发布与安全

-   基于 Tauri 2、Node.js 22 与 pnpm 10。
-   Windows 10/11 x64 NSIS 安装包。
-   移除旧 Pot 更新器、插件市场、云备份和远程 TTS 发布链路。
-   收紧生产 CSP，Release 构建不开放 WebView 开发工具。
-   增加 GPL、上游归属、第三方组件、安全策略和可复现构建文档。
